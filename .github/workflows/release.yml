name: Release

# Cuts a GitHub Release when a version tag is pushed (e.g. v0.1.0).
#
# umbra-reviewer is SOURCE-AVAILABLE (All Rights Reserved) and is NOT published to
# PyPI — it is installed from source by tag:
#   pip install "umbra-reviewer @ git+https://github.com/bkd-dotcom/umbra-reviewer@v0.1.0"
# The former PyPI Trusted-Publishing job was removed on the source-available
# lockdown. This workflow only builds/tests and attaches the built artifacts to a
# GitHub Release for convenience.
#
# Then: git tag v0.1.0 && git push origin v0.1.0

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Verify the tag matches the package version
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          PKG=$(grep -m1 '^version' pyproject.toml | sed -E 's/.*"([^"]+)".*/\1/')
          echo "tag=$TAG pkg=$PKG"
          if [ "$TAG" != "$PKG" ]; then
            echo "::error::Tag v$TAG does not match pyproject version $PKG"; exit 1
          fi

      - name: Test before releasing
        run: |
          uv sync --extra dev
          uv run ruff check umbra_reviewer/ tests/
          uv run pytest -q

      - name: Build sdist + wheel
        run: uv build

      - name: Check distribution metadata
        run: uvx twine check dist/*

      - name: Upload build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: dist
          path: dist/

  github-release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write   # create the GitHub Release
    steps:
      - uses: actions/checkout@v5

      - name: Download build artifacts
        uses: actions/download-artifact@v5
        with:
          name: dist
          path: dist/

      - name: Extract release notes from CHANGELOG
        run: |
          VER="${GITHUB_REF_NAME#v}"
          if [ -f CHANGELOG.md ]; then
            awk -v ver="$VER" '
              $0 ~ "^## \\[" ver "\\]" {grab=1; next}
              grab && /^## / {exit}
              grab {print}
            ' CHANGELOG.md > notes.md
          fi
          if [ ! -s notes.md ]; then
            echo "Release ${GITHUB_REF_NAME}. See CHANGELOG.md." > notes.md
          fi
          echo "" >> notes.md
          echo "Source-available (All Rights Reserved); install from source:" >> notes.md
          echo '```' >> notes.md
          echo "pip install \"umbra-reviewer @ git+https://github.com/bkd-dotcom/umbra-reviewer@${GITHUB_REF_NAME}\"" >> notes.md
          echo '```' >> notes.md
          echo "Notes for ${GITHUB_REF_NAME}:"; cat notes.md

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            --title "umbra-reviewer ${GITHUB_REF_NAME}" \
            --notes-file notes.md \
            --verify-tag \
            dist/* \
            || gh release edit "${GITHUB_REF_NAME}" --notes-file notes.md
